import React from "react";
import ReactDOM from "react-dom";

class App extends React.Component {
  render() {
    return (
      <section>
        Is this thing on?
      </section>
    );
  }
}

App.propTypes = {};

App.defaultProps = {};

export default App;
